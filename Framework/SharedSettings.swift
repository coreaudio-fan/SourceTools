///	Names shared between the app and the extension through the app-group defaults suite.
public enum SharedSettings {

	///	The app-group identifier both processes declare; macOS app groups carry the team-identifier prefix.
	public static let suiteName = "56N2U6EQZQ.coreaudio-fan.SourceTools"

	///	The defaults key holding the `WidthMode` raw value.
	public static let widthModeKey = "widthMode"

	///	The defaults key holding the custom wrap column.
	public static let customWidthKey = "customWidth"

}
