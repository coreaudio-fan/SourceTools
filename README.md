# SourceTools

A macOS app hosting **SourceTools for Xcode**, an Xcode Source Editor Extension that reflows the selected lines of text at a column width, preserving indentation and comment leaders.

Scaffold in progress.
